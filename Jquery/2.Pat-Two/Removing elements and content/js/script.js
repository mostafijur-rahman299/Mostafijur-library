$(function(){
    
    // $('li').remove();
    // $('form').children('input:text, textarea').remove();

    // var detachedListItem = $('li').detach();
    // $('#content').append(detachedListItem);

    // $('p:first').empty();
    $('.red-box, .blue-box, .green-box').empty();


})