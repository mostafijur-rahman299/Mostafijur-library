"""Instead of using f as the type designator,
 you can use the % symbol to format a floatingpoint number as a percentage.
 The % symbol causes the number to be multiplied by 100 and
 displayed with a % sign following it. Here is an example:"""

print(format(0.5,'%'))

print(format(0.5,'.%'))
