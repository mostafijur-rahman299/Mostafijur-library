endswith(substring)    "The substring argument is a string. \
                        The method returns true if the stringends with substring."

find(substring)        """The substring argument is a string.
                       The method returns the lowest index in the string where substring is found.
                       If substring is not found, the method returns -1."""

replace(old, new)   """The old and new arguments are both strings.
                       The method returns a copy of
                       the string with all instances of old replaced by new. """

startswith(substring) "The substring argument is a string. The method returns true \
                        if the string starts with substring."
